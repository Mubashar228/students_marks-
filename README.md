# 🎓 Student Exam Appearance Predictor

Simple ML model to predict if a student will appear in the exam based on marks.

## Run Locally

```bash
pip install -r requirements.txt
streamlit run app.py
